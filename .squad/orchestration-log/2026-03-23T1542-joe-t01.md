# Task T-01: Dockerfile + Helm Templates

**Status:** ✅ Completed  
**Assigned to:** Joe (DevOps/Infra)  
**Timestamp:** 2026-03-23T15:42:00Z  
**Commit:** c91915d

## Task
Create Dockerfile and Helm chart base templates for all 8 services. Multi-stage build pattern, non-root execution (UID 1654), security hardening (readOnlyRootFilesystem, drop ALL capabilities, no privileged escalation).

## Deliverables
- `docs/templates/Dockerfile.template` — multi-stage build (SDK → publish → aspnet:9.0 runtime), PublishReadyToRun enabled, health check via wget
- `docs/templates/helm-base/` — base Helm chart values/templates with service accounts (create: false), workload identity labels, security context, HPA disabled by default, ConfigMap checksum for rollout triggers
- `docs/templates/README.md` — usage guide for service developers to copy templates and customize per service

## Security Features
- Non-root execution (UID 1654 from aspnet:9.0)
- Read-only root filesystem with /tmp emptyDir
- Capabilities: drop ALL, no privilege escalation
- Workload identity integration enabled
- Resource limits and requests defined
- OCI labels for registry/scanner integration

## Implementation Notes
- Build context is repo root (Directory.Build.props accessible)
- Blazor UI requires modified Dockerfile pattern (nginx or static file server); base template for .NET API services only
- Helm charts will be deployed per service under `infra/helm/{service-name}/` when each service is built

## Blocked By
Nothing

## Blocks
- T-02 (NetworkPolicy validation) — requires templates to generate pod labels
